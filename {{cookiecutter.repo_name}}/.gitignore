*.egg-info
__pycache__/
*.pyc
.pixi
.vscode
.DS_Store
.ipynb_checkpoints

# Machine-local path overrides (see CLAUDE.md -> Configuration)
config.local.yaml

# Snakemake + smokescreen scratch
.snakemake/
*/.smokescreen/

# Intermediate output (regenerable from data/ via the pipeline). The
# directory itself stays (workspace/README.md is tracked).
workspace/*
!workspace/README.md
